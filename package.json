{
  "name": "web-automation-electron",
  "main": "dist/main.js",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build:client": "tsc && vite build",
    "build:main": "tsc --project tsconfig.electron.json",
    "build": "yarn build:client && yarn build:main",
    "dev:client": "vite",
    "dev:main": "tsc-watch --noClear -p tsconfig.electron.json --onSuccess \"yarn start:electron\"",
    "package": "yarn build && electron-builder",
    "preview": "vite preview",
    "start:electron": "NODE_ENV=development electron ./dist/main.js",
    "start:main": "yarn build:main && yarn start:electron"
  },
  "dependencies": {
    "playwright": "^1.21.1"
  },
  "devDependencies": {
    "@types/react-dom": "^18.0.0",
    "@types/react": "^18.0.0",
    "@vitejs/plugin-react": "^1.3.0",
    "electron-builder": "^23.0.3",
    "electron": "^18.2.0",
    "react-dom": "^18.0.0",
    "react": "^18.0.0",
    "ts-node": "^10.7.0",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.6.3",
    "vite": "^2.9.7"
  },
  "build": {
    "appId": "com.jungdujang.web-automation",
    "files": [
      "dist/**/*",
      "node_modules/**/*",
      "package.json"
    ],
    "directories": {
      "buildResources": "resources",
      "output": "packaged"
    },
    "publish": null
  }
}
